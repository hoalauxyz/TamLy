{
  "name": "@tamly/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "API cho app: check-in, sàng lọc, chat với An (pipeline an toàn), nhóm có tiền kiểm duyệt, tài nguyên khẩn cấp, Safety Ops.",
  "scripts": {
    "dev": "node --watch --env-file-if-exists=.env src/server.ts",
    "start": "node --env-file-if-exists=.env src/server.ts",
    "typecheck": "tsc --noEmit -p ."
  },
  "dependencies": {
    "@fastify/cors": "^11.0.1",
    "@tamly/core": "*",
    "fastify": "^5.3.3"
  }
}
